#include <iostream>

enum Trit{False, Unknown, True};

class TritSet {
public:
	TritSet(const int size) {
		this->size = size;
	}
	const int capacity() {
		return size * 2 / 8 / sizeof()
	}
private:
	int size;
};

int main() {

	return 0;
}